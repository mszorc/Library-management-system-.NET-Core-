
<style>
    h1 {
        font-family: 'Lato', monospace;
    }

    body {
    }
</style>
@model IEnumerable<SBDlibrary.Models.Wydawnictwa>

@{
    ViewData["Title"] = "Wydawnictwa";
}


<h1>Wydawnictwa</h1>
<hr />
<div>
    <a class="btn btn-outline-secondary" asp-action="Stworz">Dodaj</a>&nbsp;&nbsp;
</div>
<br />
<div style="text-align:center ">
    <form asp-action="Index">
        <p>
            Nazwa: <input type="text" name="nazwa" size="80">&nbsp;
            <input type="submit" value="Filtruj" />&nbsp;
            @Html.ActionLink("Wyłącz filtry", "Index")
        </p>
    </form>
</div>
<br />
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nazwa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.nazwa)
                </td>
                <td>
                    @Html.ActionLink("Edytuj", "Edytuj", new { id = item.id_wydawnictwa }) |
                    @Html.ActionLink("Usuń", "Usun", new { id = item.id_wydawnictwa })
                </td>
            </tr>
        }
    </tbody>
</table>